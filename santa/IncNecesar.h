#include <iostream>
#include <string>
#include <vector>
#include <fstream>
#include "Classes.h"
#include "Executare.h"
#include "Workers.h"


//citire din fisier
	/*
	ifstream mf1("Mosu1.txt");
	string line;
	if (mf1.is_open())
	{
		while (getline(mf1, line))
		{

			cout << line << endl;

		}
	}
	else
	{
		cout << "nu exista\n";
	}
	*/
	//using namespace std;
